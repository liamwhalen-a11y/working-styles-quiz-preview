# Working Styles — preview

Passphrase-gated static preview. The page content is **encrypted client-side**
(PBKDF2 → AES-256-GCM) and can't be read without the passphrase — the repo holds
only the ciphertext. Served via GitHub Pages.
